# Local development uses the public XMTP development environment.
VITE_XMTP_ENV=dev

# Production/mainnet messaging fails closed unless a payer Gateway hostname is
# built into the client. This value is public browser configuration, never a
# credential. Gateway authentication is still an explicit release blocker.
# VITE_XMTP_GATEWAY_HOST=gateway.example.com
